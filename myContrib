php, c#, swift, Scala file support

optimization of algorithm for checking if a file has been signed (checking the 0-50th index, instead of the whole string)

optimization of insertion of signature in the file, using the deque data structure, that reduces time complexity from O(n) to O(1)

